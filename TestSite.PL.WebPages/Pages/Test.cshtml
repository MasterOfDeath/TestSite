<!DOCTYPE html>

@using TestSite.PL.WebPages.Models
@{
    if (!User.Identity.IsAuthenticated)
    {
        Response.StatusCode = 401;
        return;
    }

    string errorMessage = null;
    var log = TestSite.Logger.Logger.Log;
    var testId = -1;

    if (!IsPost)
    {
        try
        {
            testId = Convert.ToInt32(Request["testid"]);
        }
        catch (Exception ex)
        {
            errorMessage = ex.Message;
            log.Error("GetEmployeeById and ListAllTests", ex);
        }
    }
}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="~/Content/bootstrap.min.css" />
    <link rel="stylesheet" href="~/Content/ProjectStyles/style.css" />
    <link rel="stylesheet" href="~/Content/ProjectStyles/test.css" />
    <title>Тестирование</title>
</head>
<body>
    <nav class="navbar navbar-default navbar-static-top">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            @if (User.Identity.IsAuthenticated)
            {
                <a href="~/" class="btn btn-default navbar-btn back-btn">Отмена</a>
                <button type="button" class="btn btn-primary navbar-btn check-test-btn" data-loading-text="Проверка...">Проверить</button>
            } 
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>

    @if (errorMessage != null)
    {
        <div class="alert alert-danger">@errorMessage</div>
    }

    <div class="container content-test" data-test-id="@testId">

        <h2 class="test-title"></h2>

        <div class="wrong-msg-container">
            <!-- for replace -->
        </div>

        <ul class="nav nav-tabs">
            <!-- for replace -->
        </ul>

        <div class="tab-content">
            <!-- for replace -->
        </div>
        
        <!-- шаблон для заголовка вкладки -->
        <ul class="hide tab-header-template">
            <li><a data-toggle="tab" href="#"></a></li>
        </ul>
        <!--  -->

        <!-- шаблон для вкладки -->
        <div id="-1" class="tab-pane fade in tab-template hide" data-question-id="-1">
            <h4 class="question-title"></h4>
            <div class="answers-container"></div>
        </div>
        <!--  -->

        <!-- шаблон для радиокнопки -->
        <div class="radio-template hide">
            <div class="radio">
                <label><input type="radio" class="check-input" name="optradio" data-answer-id="-1"></label>
            </div>
        </div>
        <!--  -->

        <!-- шаблон для чекбокса -->
        <div class="checkbox-template hide">
            <div class="checkbox">
                <label><input type="checkbox" class="check-input" data-answer-id="-1"></label>
            </div>
        </div>
        <!--  -->
        
        <!-- шаблон для схемы -->
        <div class="hide schema-container schema-container-template">
            <button class="btn btn-default zoom-in-btn">Zoom in</button>
            <button class="btn btn-default reset-btn">Reset</button>
            <button class="btn btn-default zoom-out-btn">Zoom out</button>
            <div class="panzoom">
                <img class="schema" src="" />
            </div>
        </div>
        <!--  -->

        <!-- шаблон не верного ответа -->
        <div class="alert alert-danger wrong-alert wrong-alert-template hide">
            <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
            <h4>Ответ не правильный! Ошибки допущены в вопросах:</h4>
            <span class="wrong-questions"></span>
        </div>

        <!-- ErrorModal -->
        @RenderPage("~/Pages/_ErrorModalPartial.cshtml")
        <!--  -->
    </div><!-- /.container -->

    <script src="~/Scripts/jquery-2.2.2.min.js"></script>
    <script src="~/Scripts/jquery.panzoom.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="~/Scripts/ProjectScripts/test.js"></script>
</body>
</html>
