1) Добавил столбец role_id в таблицу employee:

CREATE TABLE "employee" (
    "id" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    "dep_id" INTEGER NOT NULL,
    "first_name" TEXT NOT NULL,
    "last_name" TEXT NOT NULL,
    "hash" BLOB NOT NULL,
    "enabled" INTEGER,
    "role_id" INTEGER NOT NULL
)

Копирование из табл. в табл.: INSERT INTO employee SELECT *, 1 FROM employee2
Далее вручную расставить role_id из табл. employee_role

2) Удалил табл. employee_role и admin